---
name: Build CI Image
on:
  push:
    branches:
      - devel
      - stable-*
    paths:
      - 'requirements/requirements.txt'
      - 'requirements/requirements_test.txt'
      - 'requirements/requirements_dev.txt'
      - 'tools/docker/Containerfile.ci'
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 06:00 UTC

permissions:
  contents: read

env:
  IMAGE_NAME: quay.io/ansible/jewel-ci

jobs:
  build-and-push:
    name: Build and push CI image
    runs-on: ubuntu-latest
    if: >-
      github.repository == 'ansible-automation-platform/aap-gateway' &&
      (github.ref_name == 'devel' || startsWith(github.ref_name, 'stable-'))
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Determine branch tag
        id: branch
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: echo "name=${BRANCH_NAME}" >> "$GITHUB_OUTPUT"

      - name: Log in to Quay.io
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Generate image tags
        id: tags
        env:
          COMMIT_SHA: ${{ github.sha }}
        run: |
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          DATE_TAG=$(date -u +%Y-%m-%d)
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "date_tag=${DATE_TAG}" >> "$GITHUB_OUTPUT"

      - name: Build CI image
        env:
          IMAGE_REF: ${{ env.IMAGE_NAME }}
          BRANCH_NAME: ${{ steps.branch.outputs.name }}
        run: make ci-image "CI_IMAGE=${IMAGE_REF}:${BRANCH_NAME}" CONTAINER_ENGINE=docker

      - name: Tag additional versions
        env:
          IMAGE_REF: ${{ env.IMAGE_NAME }}
          BRANCH_NAME: ${{ steps.branch.outputs.name }}
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
          DATE_TAG: ${{ steps.tags.outputs.date_tag }}
        run: |
          docker tag "${IMAGE_REF}:${BRANCH_NAME}" "${IMAGE_REF}:${BRANCH_NAME}-${SHORT_SHA}"
          docker tag "${IMAGE_REF}:${BRANCH_NAME}" "${IMAGE_REF}:${BRANCH_NAME}-${DATE_TAG}"

      - name: Push all tags
        env:
          IMAGE_REF: ${{ env.IMAGE_NAME }}
          BRANCH_NAME: ${{ steps.branch.outputs.name }}
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
          DATE_TAG: ${{ steps.tags.outputs.date_tag }}
        run: |
          docker push "${IMAGE_REF}:${BRANCH_NAME}"
          docker push "${IMAGE_REF}:${BRANCH_NAME}-${SHORT_SHA}"
          docker push "${IMAGE_REF}:${BRANCH_NAME}-${DATE_TAG}"
