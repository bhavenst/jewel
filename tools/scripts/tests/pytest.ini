[pytest]
# Standalone pytest config for script tests (no Django)
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = .
# Don't inherit Django settings from parent
addopts = -p no:django

